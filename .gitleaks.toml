[extend]
useDefault = true

[allowlist]
description = "Test fixtures use obviously-fake placeholder credentials."
paths = [
  '''tests/.*''',
  '''\.env\.example''',
  '''\.mypy_cache/.*''',
  '''\.ruff_cache/.*''',
]
